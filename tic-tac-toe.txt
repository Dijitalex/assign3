Attempted to create a 2nd box for tic-tac-toe by duplicating code:
- Resulted in the webpage crashing, I suspect it could be due to there being no specification about what this button is or where it's meant to go.
- Once the markers <></> were added it works, but it automatically formats them moving to the right as a continuous line.

Added in a 3x3 grid of boxes:
- Using <div> to act as mini-containers for each row, I am successfully able to stack 3 rows of 3 on top of each other to make a 3x3 grid of boxes with numbers 1-9.
- I suspect the reason why this works is because of the formating in CSS, which I would really like to learn more about but unfortunately it appears that's not a focus of this class. ;-;

Reworked square() function:
- Now the code is much cleaner, but now arises the issue of the board being all 1's for now.
- Okay and now it's solved by passing an argument into the function
- However when we implement it as {value} each box has the string literal "value"
- Now it's been fixed with more {}, and I was able to implement the numbers on my own!

Making each square a button:
- By nesting a click function, I am able to have console.log register clicks (needed to open f12 to see it on my own environment)
- After implementing value and setValue, they are updated upon the click of the button.

Redefining the structure to support X's and O's:
- Each square now has an onSquareClick component to it now, passing the function handleClick()
- Now upon each click a single X gets entered in the array's position 0, but this makes sense as only nextSqures[0] has been implemented so far.
- After implementing the arrow function and pasing an argument to the function, we are now back to the original state!
- Implementing an if else statement allows alternating between X's and O's, but overriding of existing squares is allowed
- Adding a quick "if it already exists" conditional remedies this.

Implementation of a way to win the game:
- Added the calcuate winner function:
 - Lists all possible winning combinations
 - Checks to see if all those values are filled with a valid type of the same letter
